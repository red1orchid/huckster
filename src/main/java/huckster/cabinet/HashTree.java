/*
package huckster.cabinet;

import java.util.HashMap;

*/
/**
 * Created by PerevalovaMA on 01.06.2016.
 *//*

class HashTree {
    private HashMap<Integer, Node> roots;

    HashTree() {
        roots = new HashMap<>();
    }

    addNode(Integer key, String data, Integer parent) {
        if (parent == null) {
            roots.put(key, new Node(data));
        } else {
            roots.computeIfPresent(key, )
        }
    }

    private Node getNode(Integer key) {

    }

    private class Node {
        private String data;
        private HashMap<Integer, Node> children;

        Node(String data) {
            this.data = data;
        }

        private Node getNode(Integer key) {

        }
    }
}*/
